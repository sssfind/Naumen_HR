# URL API без прокси (опционально). По умолчанию /api/v1 через Vite proxy.
# VITE_API_BASE_URL=http://localhost:8080/api/v1

# Цель прокси Vite (для docker compose — http://app:8080)
# VITE_API_PROXY_TARGET=http://localhost:8080
